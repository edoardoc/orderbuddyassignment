# ✅ Task Board

This file tracks all development tasks. Each task should be:

- Scoped and clear
- Written in natural language
- Tied to a specific page, component, or feature
- Marked `[x]` when complete and merged

---

## 🧭 Menu Page

- [ ] Scaffold `MenuPage.tsx` layout with header, category blocks, and floating cart summary
- [ ] Create reusable `MenuCategory` component to group items by category
- [ ] Create `MenuItemCard` component to display item name, image, price, and add to cart button
- [ ] Add dummy menu data to simulate restaurant menu structure
- [ ] Wire `MenuPage` to use local dummy data and render category + item layout

---

## 🚧 Next Feature Area
(Leave this section blank for now — more to come after MenuPage)