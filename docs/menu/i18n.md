//get user language
const browserLang = navigator.language || navigator.userLanguage; // e.g., "en-US"
const baseLang = browserLang.split("-")[0]; // "en"

//save in state and localStorage
function getUserLang() {
return localStorage.getItem("ob.lang") || getBrowserLang() || "en";
}

//translate function, will be using global hook in near future
function t(i18nField: Record<string, string>, lang: string): string {
return i18nField[lang] || i18nField["en"];
}

<span>{t(menuItem.name, currentLang)}</span>
